import PropTypes from 'prop-types';
import React from 'react';
import Layout from '../../components/Layout/Index';
import styles from './album.module.css';


const Album = () => {
  return (
    <Layout>This is an album</Layout>
  );
};

Album.propTypes = {
};

export default Album;